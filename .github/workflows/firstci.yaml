
name: GitHub Actions Demo
on:  [ push ]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
     -run: echo " The job is completed by a ${{ github.event.name }} event."
     -run: python main.py